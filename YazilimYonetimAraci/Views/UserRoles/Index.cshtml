@model IEnumerable <YazilimYonetimAraci.Models.ModelUserRolesAndRoleNames>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
        <div class="container">
            <div class="row">
                <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                    <table class="table">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.UserRoles.UserRolesID)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.UserRoles.UserID)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.UserRoles.RoleNameID)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.RoleNames.RoleName)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.UserRoles.UserRolesID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.UserRoles.UserID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.UserRoles.RoleNameID)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.RoleNames.RoleName)
                                </td>


                                <td>
                                    @Html.ActionLink("Kullanıcı Rolü Seç", "RoleSet", new { RoleNameID=item.UserRoles.RoleNameID})
                                    @*<div class="form-group">
                                        <div class="col-md-offset-2 col-md-10">
                                            <input type="submit" value="Rol Seç" class="btn btn-success" />
                                        </div>
                                    </div>
                                    <button type="submit">Rol Seç</button>*@
                                </td>
                            </tr>
                        }

                    </table>
                </div>

            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
